# بوت ريحانة المغرب الأوسط «2»

بوت تيليجرام مكتوب بـ Python يدير نشر صفحات مجلة PDF تلقائياً مع رسائل تحفيزية كل 3 ساعات عبر Groq AI.

## المكدس التقني
- **Python 3.11**
- **python-telegram-bot 21.3** — تشغيل البوت
- **PyMuPDF** — استخراج صفحات PDF كصور
- **Groq AI (llama-3.3-70b)** — توليد النصوص والتعليقات
- **APScheduler** — جدولة المهام الدورية

## المتغيرات البيئية المطلوبة

| المتغير | الوصف |
|---|---|
| `TELEGRAM_TOKEN` | توكن البوت من @BotFather (**مطلوب**) |
| `GROQ_API_KEY` | مفتاح Groq AI (**مطلوب**) |
| `CHANNEL_USERNAME` | اسم القناة (افتراضي: `@Athar_Dz_Islamic`) |
| `PDF_PATH` | مسار ملف المجلة (افتراضي: `magazine.pdf`) |

## التشغيل على Railway
المشروع جاهز للنشر على Railway:
- `Procfile` يشغّل البوت عبر: `python main.py`
- `nixpacks.toml` يحدد Python 3.11

أضف المتغيرات البيئية في لوحة تحكم Railway قبل النشر.

## ميزات البوت
1. **نشر المجلة** — صفحة صباحاً (09:00) وصفحة مساءً (20:00) بتوقيت الجزائر
2. **منشور دوري** — كل 3 ساعات عبر Groq AI مع التذييل الإجباري
3. **تعليق تلقائي** — على منشورات القناة في المجموعة المرتبطة
4. **مساعد شرعي** — يرد على أسئلة الأعضاء في المجموعة

## تتبع الصفحات
يُخزَّن رقم الصفحة الحالي في `current_page.txt` لضمان الاستمرارية بدون تكرار.
